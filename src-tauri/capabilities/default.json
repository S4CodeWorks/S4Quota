{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main and compact surfaces",
  "windows": ["main", "compact"],
  "permissions": ["core:default"]
}
