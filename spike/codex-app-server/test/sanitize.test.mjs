import test from "node:test"; import assert from "node:assert/strict"; import { sanitizeText, summarizeAccount, summarizeRateLimits } from "../src/sanitize.mjs";
test("redacts common credential and identity shapes", () => { const value = sanitizeText("user@example.com sk-abcdefghijklmnop eyJabcdefghijklmnop.qwerty"); assert.equal(value.includes("user@example.com"), false); assert.equal(value.includes("sk-abcdefghijklmnop"), false); assert.equal(value.includes("eyJabcdefghijklmnop"), false); });
test("account summary records presence without preserving identity", () => { assert.deepEqual(summarizeAccount({ account: { type: "chatgpt", email: "user@example.com", planType: "pro" }, requiresOpenaiAuth: true }), { accountPresent: true, accountType: "chatgpt", planTypePresent: true, emailPresent: true, requiresOpenaiAuth: true }); });
test("quota summary omits exact percentages and reset timestamps", () => { const summary = summarizeRateLimits({ rateLimits: { limitId: "codex", primary: { usedPercent: 37, windowDurationMins: 300, resetsAt: 4102444800 } } }, 1); const serialized = JSON.stringify(summary); assert.equal(serialized.includes("37"), false); assert.equal(serialized.includes("4102444800"), false); assert.equal(summary.buckets[0].primary.usedPercentRangeValid, true); });
