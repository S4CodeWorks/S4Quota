import test from "node:test"; import assert from "node:assert/strict"; import { codexCommand, discoverCodex, readCodexVersion } from "../src/discovery.mjs";
test("rejects an invalid explicit executable without falling back", async () => { const previous = process.env.CODEX_SPIKE_EXECUTABLE; process.env.CODEX_SPIKE_EXECUTABLE = "Z:\\does-not-exist\\codex.exe"; try { await assert.rejects(discoverCodex(), /does not exist/); } finally { if (previous === undefined) delete process.env.CODEX_SPIKE_EXECUTABLE; else process.env.CODEX_SPIKE_EXECUTABLE = previous; } });
test("treats version output as diagnostic data", async () => { assert.equal(await readCodexVersion(process.execPath), "unrecognized"); });
test("wraps cmd shims without enabling a general shell", { skip: process.platform !== "win32" }, () => { const command = codexCommand("C:\\trusted\\codex.cmd", ["app-server"]); assert.equal(command.executable.toLowerCase().endsWith("cmd.exe"), true); assert.deepEqual(command.args, ["/d", "/c", "call", "C:\\trusted\\codex.cmd", "app-server"]); });
